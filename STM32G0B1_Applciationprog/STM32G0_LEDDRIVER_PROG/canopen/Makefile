##########################################################################################################################
# File automatically-generated by tool: [projectgenerator] version: [4.8.0-B50] date: [Fri Dec 05 16:27:43 SGT 2025]
##########################################################################################################################

# ------------------------------------------------
# Generic Makefile (based on gcc)
#
# ChangeLog :
#	2017-02-10 - Several enhancements + project update mode
#   2015-07-22 - first version
# ------------------------------------------------

######################################
# target
######################################
TARGET = libcanopen


######################################
# building variables
######################################
# debug build?
DEBUG ?= 1
# optimization
OPT_DEBUG = -Og -g
OPT_RELEASE = -O2

#######################################
# paths
#######################################
# Build path
DEBUG_SUBDIR := debug
RELEASE_SUBDIR := release
BUILD_DIR = build
BUILD_DIR_DEBUG = $(BUILD_DIR)/$(DEBUG_SUBDIR)
BUILD_DIR_RELEASE = $(BUILD_DIR)/$(RELEASE_SUBDIR)
BIN_DIR = install

######################################
# source
######################################
# C sources
C_TEMPLATES = $(wildcard STM32CubeF3/Drivers/STM32F3xx_HAL_Driver/Src/*_template.c)
C_SOURCES = \
	$(wildcard Src/**/*.c) \
	$(wildcard Src/*.c)

# ASM sources
ASM_SOURCES =   \
startup_stm32f303xe.s

# ASMM sources
ASMM_SOURCES = \



#######################################
# binaries
#######################################
PREFIX = arm-none-eabi-
# The gcc compiler bin path can be either defined in make command via GCC_PATH variable (> make GCC_PATH=xxx)
# either it can be added to the PATH environment variable.
ifdef GCC_PATH
CC = $(GCC_PATH)/$(PREFIX)gcc
AS = $(GCC_PATH)/$(PREFIX)gcc -x assembler-with-cpp
CP = $(GCC_PATH)/$(PREFIX)objcopy
SZ = $(GCC_PATH)/$(PREFIX)size
AR = $(GCC_PATH)/$(PREFIX)ar
else
CC = $(PREFIX)gcc
AS = $(PREFIX)gcc -x assembler-with-cpp
CP = $(PREFIX)objcopy
SZ = $(PREFIX)size
AR = $(PREFIX)ar
endif
HEX = $(CP) -O ihex
BIN = $(CP) -O binary -S

#######################################
# CFLAGS
#######################################
# cpu
CPU = -mcpu=cortex-m4

# fpu
FPU = -mfpu=fpv4-sp-d16

# float-abi
FLOAT-ABI = -mfloat-abi=hard

# mcu
MCU = $(CPU) -mthumb $(FPU) $(FLOAT-ABI)

# macros for gcc
# AS defines
AS_DEFS =

# C defines
C_DEFS =   \
-DSTM32F303xE \


# AS includes
AS_INCLUDES = \

# C includes
C_INCLUDES = \
-IInc
# -IDrivers/STM32F3xx_HAL_Driver/Inc \
# -IDrivers/STM32F3xx_HAL_Driver/Inc/Legacy \
# -IDrivers/CMSIS/Device/ST/STM32F3xx/Include


# compile gcc flags
ASFLAGS = $(MCU) $(AS_DEFS) $(AS_INCLUDES) -Wall -fdata-sections -ffunction-sections

CFLAGS += $(MCU) $(C_DEFS) $(C_INCLUDES) -Wall -fdata-sections -ffunction-sections

# ifeq ($(DEBUG), 1)
# CFLAGS += -g -gdwarf-2
# endif


# Generate dependency information
CFLAGS += -MMD -MP -MF"$(@:%.o=%.d)"


#######################################
# LDFLAGS
#######################################
# link script
LDSCRIPT = STM32F303XX_FLASH.ld

# libraries
LIBS = -lc -lm -lnosys
LIBDIR =
LDFLAGS = $(MCU) -specs=nano.specs -T$(LDSCRIPT) $(LIBDIR) $(LIBS) -Wl,-Map=$(BUILD_DIR)/$(TARGET).map,--cref -Wl,--gc-sections

# ARCHIVES
AR_DEBUG = $(BIN_DIR)/$(DEBUG_SUBDIR)/$(TARGET).a
AR_RELEASE = $(BIN_DIR)/$(RELEASE_SUBDIR)/$(TARGET).a
BIN_DIR_DEBUG = $(BIN_DIR)/$(DEBUG_SUBDIR)
BIN_DIR_RELEASE = $(BIN_DIR)/$(RELEASE_SUBDIR)

#######################################
# build the application
#######################################
# list of objects
OBJECTS_DEBUG = $(addprefix $(BUILD_DIR_DEBUG)/,$(notdir $(C_SOURCES:.c=.o)))
OBJECTS_RELEASE = $(addprefix $(BUILD_DIR_RELEASE)/,$(notdir $(C_SOURCES:.c=.o)))
vpath %.c $(sort $(dir $(C_SOURCES)))

.PHONY: all
all: debug release

.PHONY: debug
debug: $(AR_DEBUG)

.PHONY: release
release: $(AR_RELEASE)

$(AR_DEBUG): $(OBJECTS_DEBUG) | $(BIN_DIR_DEBUG)
	$(AR) rcs $(AR_DEBUG) $^

$(AR_RELEASE): $(OBJECTS_RELEASE) | $(BIN_DIR_RELEASE)
	$(AR) rcs $(AR_RELEASE) $^

$(BUILD_DIR_DEBUG)/%.o: %.c Makefile | $(BUILD_DIR_DEBUG)
	$(CC) -c $(CFLAGS) $(OPT_DEBUG) -Wa,-a,-ad,-alms=$(BUILD_DIR)/$(notdir $(<:.c=.lst)) $< -o $@

$(BUILD_DIR_RELEASE)/%.o: %.c Makefile | $(BUILD_DIR_RELEASE)
	$(CC) -c $(CFLAGS) $(OPT_RELEASE) -Wa,-a,-ad,-alms=$(BUILD_DIR)/$(notdir $(<:.c=.lst)) $< -o $@

$(BUILD_DIR_DEBUG):
	mkdir -p $@

$(BUILD_DIR_RELEASE):
	mkdir -p $@

$(BIN_DIR_DEBUG):
	mkdir -p $@

$(BIN_DIR_RELEASE):
	mkdir -p $@

#######################################
# clean up
#######################################
clean:
	-rm -fR $(BUILD_DIR)
	-rm -fR $(BIN_DIR)
	-rm -fR bin

#######################################
# dependencies
#######################################
-include $(wildcard $(BUILD_DIR)/*.d)

# *** EOF **
# testing stuff
build_native_test:
	/usr/bin/make -e -f test.mk $@

# all including tests
all_and_tests: build_native_test
run_native_test: build_native_test
	/usr/bin/make -e -f test.mk $@
